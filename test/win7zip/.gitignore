# build output (compil.bat and the .dproj both write here)
/build/
# Delphi IDE build artifacts, if output is left at defaults
*.dcu
*.exe
*.map
# auto-generated default app resource (the .dpr has no {$R *.res}; not needed)
*.res
*.drc
*.local
*.identcache
__history/
__recovery/
Win32/
Win64/
# scratch folder created by the test at run time
/win7ziptest_work/
